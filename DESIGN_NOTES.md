# 项目设计笔记

> 本文档记录跨技能的非显而易见决策，防止 Agent 在不同会话间重新做出已否决的选择。只记录"为什么"，不重复 AGENTS.md 中的"是什么"。

## 为什么三个技能必须独立部署

AI 编程工具的技能系统（`.claude/skills/`、`.agents/skills/`）要求技能从自身目录运行。用户安装技能时只复制技能目录，仓库结构不复存在。因此:

- 不能假定仓库根路径存在
- 不能假定技能路径为 `skills/novel-*`
- 所有命令必须使用 `<skill-root>` 占位符

**后果**: 即使两个技能使用相似的校验逻辑，也不抽取到共享模块。宁可容忍少量重复，也要保证部署独立性。

## 为什么技能间用产物文件而非代码 import 通信

调研 → 起草 → 交付是**单向流水线**。每阶段读取上一阶段的产物文件，推进后写回结果:

```
00-project/ + 20-story/ → novel-drafting → 30-draft/ + 40-review/
30-draft/ + 40-review/  → novel-delivery  → 50-delivery/
```

产物文件契约在 `skills/novel-workflow-overview.md` 中定义。这种"文件即 API"的架构避免了技能间的代码耦合。

## 为什么校验器必须输出四段式错误

每个校验失败消息必须包含:
1. **什么出错了** — 具体路径和字段
2. **为什么重要** — 对下游的影响
3. **如何修复** — 可操作步骤
4. **在哪里查找** — 相关文档位置

这是 Agent 友好设计：Agent 收到错误后应能**自主修复**，无需人工解释。

## 为什么 `strict: false`

`tsconfig.json` 中 `strict` 为 `false`。原因是项目中混合了 `.mts`、`.js`、`.mjs` 文件类型，部分技能脚本运行在 `--experimental-strip-types` 模式下，严格检查会破坏现有的 JS 互操作模式。待 TS 迁移完成后将开启 strict 模式。

## 为什么 AGENTS.md 用条件路由表而非文件列表

早期版本的 "Where To Look" 用被动列举，Agent 不一定遵循。改为触发式路由表：每行定义触发场景 → 必须阅读的文档 → 原因。这与渐进式披露一致——在问题发生时引入知识，而非预先堆砌。

## 为什么用 `.mts` 而非 `.ts`

显式 ESM 模式。Node.js 的 `--experimental-strip-types` 默认按 `.mts` → ESM 解析。使用 `.mts` 消除了 `package.json` 中 `"type": "module"` 需求，与技能可能部署到的任何 Node.js 项目兼容。

## 为什么禁止 `as any` / `@ts-ignore`

如果 Agent 无法让类型正常工作，说明设计有问题。用类型断言掩盖问题会导致运行时崩溃——更糟的是，这些崩溃只有部署到用户环境后才会暴露。
